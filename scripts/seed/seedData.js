/**
 * Seed script - run after DBs are up.
 * Usage: node scripts/seed/seedData.js (from repo root after npm install)
 * Expand with user/ride data as needed.
 */
function main() {
  return Promise.resolve('Seed script placeholder - add user/ride seeds as needed');
}
main().then((msg) => process.stdout.write(msg + '\n'));
